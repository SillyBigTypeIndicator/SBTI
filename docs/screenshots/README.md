# Screenshots (placeholders)

Add launch assets here for the GitHub README:

1. **landing.png** — hero + CTA on `/`
2. **test-flow.png** — question card + progress on `/test`
3. **result.png** — result page with type card on `/result/EOVA`
4. **compare.png** — compare view with radar chart on `/compare`

Replace filenames in the main README when images exist.
